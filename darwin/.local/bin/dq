#!/bin/sh

# Remove macOS quarantine metadata from explicitly selected paths.

set -eu

if [ "$#" -eq 0 ]; then
    printf '%s\n' 'usage: dq PATH [...]' >&2
    exit 2
fi

xattr -r -d com.apple.quarantine "$@"
